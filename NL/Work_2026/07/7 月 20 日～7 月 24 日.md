修复 点击多次 生成报告菜单 会弹出多个界面问题

1、打包注册版本
2、加密、修改时间
3、生成最终安装包


阶段 1（1~2 周）
├── 统一菜单 Command（去掉 MainWindow.ui 硬编码 Action）
├── 报告/备份/导入 拆成独立 Service
└── DataManagerWidget 只保留 View + Presenter 调用

阶段 2（2~4 周）
├── AppContext 依赖注入，减少新 Singleton
├── DataManagerController 拆服务
└── Pipeline StepHandler 注册表化

阶段 3（长期）
├── Session 状态机
├── Repository + Undo/Redo
└── EventBus 类型化 / 插件只依赖接口